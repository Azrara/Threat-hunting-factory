fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
python-multipart>=0.0.9
bcrypt>=4.1
pyjwt>=2.8
reportlab>=4.0
pytest>=8.0
httpx>=0.27
# Optional: unlocks parsing of binary Windows event logs (.evtx) directly.
python-evtx>=0.7
# Optional: lets the hypothesis collector read research papers published as PDF.
# Without it, PDF sources are skipped and everything else works unchanged.
pypdf>=4.0
